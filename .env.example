# Local development only. Copy to .env before changing values.
# Production must set APP_ENV=production and supply unique, non-demo secrets.
APP_ENV=development
POSTGRES_DB=visionops
POSTGRES_USER=visionops
POSTGRES_PASSWORD=visionops
DATABASE_URL=postgres://visionops:visionops@db:5432/visionops?sslmode=disable
JWT_SECRET=local-development-secret-change-me
INGEST_API_KEY=vo_demo_ingest
